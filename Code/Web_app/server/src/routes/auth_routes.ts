import { Router } from "express";

const router = Router();

// Users routes
router.post("/signup", () => {});
router.post("/login", () => {});
router.post("/logout", () => {});

export default router;
