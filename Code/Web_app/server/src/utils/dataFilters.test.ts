import { removePasswordInUser } from "./dataFilters";

describe("dataFilters", () => {
  describe("removePasswordInUser", () => {
    it("return a User object with the attr password removed", () => {
      expect(removePasswordInUser()).toBe();
    });
  });
});
